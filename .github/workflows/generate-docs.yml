name: GFT AI Generate Documents

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  run-generator:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout código
        uses: actions/checkout@v3

      - name: Exibir conteúdo do projeto
        run: ls -R

      # Adicione aqui seus scripts de build ou geração
